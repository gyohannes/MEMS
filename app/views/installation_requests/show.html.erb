<div class="header">
  <h2>Installation Request</h2>
</div>
<div class="body">
  <div class="row">
    <div class="col-sm-6">
      <div class="row">
        <label class="col-sm-6">Request By:</label>
        <%= @installation_request.user.organization_unit %>
      </div>

      <div class="row">
        <label class="col-sm-6">Request Date:</label>
        <%= @installation_request.request_date.strftime("%B %d, %Y") rescue nil %>
      </div>

      <p class="row">
        <label class="col-sm-6">Equipment Name:</label>
        <%= @installation_request.equipment_name %>
      </p>

      <p class="row">
        <label class="col-sm-6">Model:</label>
        <%= @installation_request.model %>
      </p>

      <p class="row">
        <label class="col-sm-6">Description:</label>
        <%= @installation_request.description %>
      </p>
      <p class="row">
        <% if @installation_request.attachment.attached? %>
            <label class="col-sm-6">Attached Document:</label>
            <%= link_to @installation_request.attachment, target: '_blank' do %>
                <button><i class="material-icons">file_download</i></button>
            <% end %>
        <% end %>
      </p>
      <p class="row">
        <label class="col-sm-6">Status:</label>
        <%= @installation_request.status %>
        <% if @installation_request.status == Constants::OUT_SOURCED %>
            to <%= @installation_request.forwards.order('created_at DESC').first.institution %>
        <% end %>
        <% if @installation_request.status == Constants::FORWARDED %>
            to <%= @installation_request.forwards.order('created_at DESC').first.organization_unit %>
        <% end %>
      </p>
      <% unless @installation_request.status==Constants::PENDING %>
          <div class="row"><label class="col-sm-6"><%= @installation_request.status %> by:</label> <%= @installation_request.decided_user.organization_unit %></div>
          <div class="row"><label class="col-sm-6">Comment:</label> <%= @installation_request.comment %></div>
      <% end %>

    </div>
    <div class="col-sm-6 left_form">
      <% if can? :decision, @installation_request and ([Constants::PENDING, Constants::FORWARDED].include?(@installation_request.status)) %>
          <%= simple_nested_form_for(@installation_request, url: decision_installation_request_path) do |f| %>
              <%= f.input :comment %>
              <%= f.input :status, collection: @actions, as: :radio_buttons, wrapper_html: {class: 'single_columned_list'} %>
              <%= f.simple_fields_for :forwards, Forward.new do |ff| %>
                  <div id="outsource">
                    <%= ff.input :institution_id, label: "Out Sourced to", collection: @institutions %>
                  </div>
                  <div id="forward">
                    <%= ff.input :organization_unit_id, label: 'Forward to', collection: @organization_units, include_blank: false %>
                  </div>
              <% end %>
              <%= f.association :decided_user, as: :hidden, input_html: {value: current_user.id} %>

              <hr/>
              <%= f.button :submit, 'Submit' %>
          <% end %>
      <% end %>
    </div>
  </div>
  <hr/>
  <%= link_to 'Back', installation_requests_path, class: 'btn btn-primary' %>
  <%= link_to 'Edit', edit_installation_request_path(@installation_request), class: 'btn btn-primary' if can? :edit, @installation_request and @installation_request.status == Constants::PENDING  %>
</div>